#include "minishell.h"

void general_exec(char **absolute_paths, t_shell *shell, char *envp[])
{
	//Check if command is directly executable or external function
	
	//Check if the command is builtin

	//Check if command is pipe

	//Command of not error
}